apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation rootProject.ext.kotlinGradle + kotlin_version
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
